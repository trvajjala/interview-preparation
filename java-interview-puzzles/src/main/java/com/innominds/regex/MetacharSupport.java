package com.innominds.regex;

/**
 *
 *
 * @author ThirupathiReddy V
 *
 */
public class MetacharSupport {

    // which of the meta characters supported by regex
    // Answer: // ( ) [ ] { { \ ^ $ | ? * + . < > - = !

    // NOTE: # @ & are not supported

}
